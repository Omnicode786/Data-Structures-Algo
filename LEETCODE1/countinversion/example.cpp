#include <bits/stdc++.h>

using namespace std;

int countinversion(vector<int> nums,vector<int> nums2, int m, int n){
    int left = 0;
    int right = 0;
    int count = 0;
    while (left < m && right < n)
    {
        if (nums[left] )
        {
            /* code */
        }
        
    }
    
}